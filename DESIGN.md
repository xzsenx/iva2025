# ИВА — DESIGN.md

Цветочная студия в Белгороде. Авторские букеты, доставка в день заказа. Премиальный минимализм с настроением старой европейской флористики.

## Brand voice

- Тон: спокойный, тёплый, чуть поэтичный. Не пафос, не «продающий». Пример: «Букет — это маленькая история», «Собираем с любовью, доставляем в день заказа».
- Слоганы серифом, курсивом можно выделить ключевое слово: «Цветы, в которых *живёт настроение*».
- Никаких эмодзи в макетах (иконки только SVG). Эмодзи допустимы только внутри админ-панели.

## Color palette

Основа — глубокий тёмно-зелёный «эвкалипт» с молочно-бежевым текстом и акцентом пыльной розы.

| Token | HEX | Использование |
|---|---|---|
| `--bg` | `#3D4F4C` | основной фон (тёмный эвкалипт) |
| `--bg-light` | `#4A5E5A` | второстепенный фон |
| `--bg-dark` | `#2F3E3B` | секции контраста, футер |
| `--bg-card` | `rgba(0,0,0,.22)` | карточки товаров |
| `--bg-glass` | `rgba(0,0,0,.30)` | стеклянные оверлеи |
| `--cream` | `#EDE6DA` | основной текст, заголовки |
| `--cream-dim` | `rgba(237,230,218,.65)` | подсказки, второстепенный текст |
| `--cream-faint` | `rgba(237,230,218,.4)` | disclaimer, hint |
| `--pink` | `#C9A4A0` | primary CTA, брендовый акцент |
| `--pink-light` | `#D4B5B1` | hover, ссылки |
| `--pink-dim` | `rgba(201,164,160,.25)` | пасивные плашки, бордер акцента |
| `--accent` | `#A8C5A0` | success (оплачено, доставлено) |
| `--gold` | `#C8B07A` | редко, для выделения статуса «премиум» |
| `--danger` | `#C97070` | ошибки, отмены |

Контраст: белый текст на фоне `--bg` даёт 8.4:1, всё AAA. Никаких «grey on grey».

## Typography

Пара шрифтов:

- **Заголовки и лого**: `'Cormorant Garamond', 'Playfair Display', Georgia, serif` — 500/600 вес, часто с italic для выделения.
- **Основной текст, UI**: `'Manrope', -apple-system, sans-serif` — 400/500/600/700, line-height 1.6.

Шкала размеров (base 16px):

| Уровень | Размер | Вес | Шрифт |
|---|---|---|---|
| Hero | `clamp(40px, 5vw, 64px)` | 500 | serif |
| H2 | `clamp(32px, 4vw, 48px)` | 500 | serif |
| H3 | 22-28px | 600 | serif |
| Body | 16px | 400 | sans |
| Small | 13-14px | 500 | sans |
| Micro | 11-12px, uppercase, letter-spacing 1.5px | 600 | sans |

Правило: заголовок серифом → подпись под ним sans в верхнем регистре с трекингом (eyebrow-паттерн).

## Layout

- Основной контейнер `max-width: 1200px`, padding 20-24px по бокам.
- Узкий контейнер (текстовые страницы, статус заказа) `max-width: 820px`.
- Мобильный: max-width 430px (Telegram Mini App).
- Сетка карточек: 3 колонки десктоп, 2 колонки мобилка, gap 20-24px.
- Отступы между секциями: 60-80px десктоп, 40-48px мобилка.

## Radius & shadows

```
--radius:    16px  /* карточки, инпуты */
--radius-sm: 10px  /* мелкие кнопки, чипы */
--radius-lg: 24px  /* большие блоки, hero */
--shadow:    0 4px 24px rgba(0,0,0,.25)
--shadow-lg: 0 12px 40px rgba(0,0,0,.35)
```

Никаких резких боксов — всё скруглено. Тени мягкие, глубокие, без чёткой границы.

## Motion

- `--transition: .25s cubic-bezier(.4,0,.2,1)` — стандартный.
- Появление секций при скролле: fadeIn + translateY(8-16px), 500-700ms.
- Пульсирующая точка активного шага таймлайна (1.8s ease-in-out infinite).
- Никаких свистопляск — движение поддерживает смысл, не украшает.

## Components

### Кнопки
- **Primary**: фон `--pink`, текст `--cream`/`#fff`, radius `--radius`, padding 14x28, вес 600.
- **Ghost**: transparent, border 1px `rgba(237,230,218,.2)`, hover — border `--cream`.
- Размеры: `sm` (padding 8x14), `default`, `lg` (padding 18x40).

### Карточка товара
- Фон `--bg-card`, radius `--radius`, aspect-ratio 3/4 для картинки.
- Заголовок sans 15-16px 600, цена serif 20-22px `--pink`.
- Хвост: кнопка «В корзину» ghost на всю ширину.

### Инпут / поле
- Радиус `--radius-sm`, фон `--bg-card`, border 1px `rgba(237,230,218,.06)`.
- Focus: border `--pink`.
- Label над полем, 12px `--cream-dim`, letter-spacing .3px.

### Тумблер (toggle-lbl)
- Трек 44x24, thumb 18x18 `--cream`.
- Активный трек: `--pink`, thumb сдвиг на 20px.

### Плашка баннера (промо)
- Стеклянная: `linear-gradient(135deg, rgba(232,220,200,.08), rgba(232,220,200,.02))`, border `rgba(232,220,200,.18)`.
- Padding 14-18px, radius `--radius`.

### Таймлайн заказа
- 5 шагов: Оплачен → Собираем → Собран → В пути → Доставлен.
- Активный шаг: заливка `--accent` (#A8C5A0), пульсация.
- Соединительная линия: 2px `rgba(237,230,218,.08)`.

### Header
- Sticky, backdrop-filter blur(12px), border-bottom 1px `rgba(255,255,255,.08)`.
- Логотип слева (изображение), навигация центрально, телефон + иконки справа.
- На мобилке: логотип + иконка корзины, навигация уходит в bottom-tabbar.

### Bottom tabbar (мобилка)
- Fixed bottom, 4 таба: Главная / Каталог / Собрать / Корзина.
- Fон `rgba(47,62,59,.94)`, backdrop-filter blur(20px).
- Активный — розовый (`--pink`), иконки SVG stroke.

## Иконки

Только SVG line-style, толщина обводки 1.8-2px, corner-radius round, размер 22x22 для UI, 16x16 для inline.

Набор: [Feather Icons] / [Lucide]. Никаких эмодзи, никаких Material filled.

## Изображения

- Фото цветов: тёплый свет, слегка недосвет, естественные тени.
- Формат WebP, размер до 900px для hero, 600px для карточек.
- Aspect ratios: hero `4/5` (портрет), карточка `3/4`, витрина `1/1`.
- Placeholder цвет: `--bg-card`.

## Тёмная схема

Проект **всегда тёмный** — светлой темы нет. Это часть бренда. Если понадобится light — сохранить пропорции: pink → тёплый терракот, cream → глубокий коричневый, зелёный → мшистый.

## Дон'ты

- ❌ Яркие насыщенные цвета (чистый красный, синий, жёлтый). Только приглушённые.
- ❌ Sans-serif для заголовков «главных экранов». Заголовок = serif.
- ❌ Резкие тени и обводки. Тени растянутые, border 1px с прозрачностью.
- ❌ Много CTA в одном экране. Одна главная кнопка, максимум одна вторичная.
- ❌ Эмодзи как иконки в UI. Только SVG.
- ❌ Тексты с ! и капсом («КУПИ СЕЙЧАС!!!»). Спокойный тон.

## Референсы

- Мобильный Zara (глубокий фон, serif, минимализм)
- Aesop web (типографика, спокойствие)
- Ohara Flowers (флористическая эстетика, пастель)
